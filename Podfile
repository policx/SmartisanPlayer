# Uncomment this line to define a global platform for your project
# platform :ios, '8.0'
# Uncomment this line if you're using Swift
# use_frameworks!

platform :ios, '8.0'  # 指定平台与版本
inhibit_all_warnings!  # 全局禁止显示警告
target 'SmartisanPlayer' do
    # Pods for MyApp
    pod 'AFNetworking' #,'<=4.0'  # AFNetworking 版本必须<=4.0
    pod 'MBProgressHUD' #, '~>1.6.0'  # Fabric 版本必须 >=1.6.0 并且 <1.7.0
    pod 'MJExtension' #, '>=1.0.0'  # ObjectiveSugar 版本必须 >=1.0.0
    pod 'MJRefresh'
    pod 'SDWebImage'
    pod 'Masonry'
    # Pods for testing
    #target 'SmartisanPlayerTests' do  # MyAppTests 目录下的引用
    #    inherit! :search_paths
    #end
    # Pods for testing
    #target 'SmartisanPlayerUITests' do # MyAppUITests 目录下的引用
    #    inherit! :search_paths
    #end
end

